filename = "generate_sshd.lua"

geoip_geo_append = {
    path = "/nonexistent",
    test = true,
    fields = { "nonexistent", "remote_addr" },
    lookup = { city = "city", country_code = "country" },
}
